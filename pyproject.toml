[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "slipstream-oracle"
version = "0.1.0"
description = "Machine Learning based Formula 1 prediction system"
authors = [
    { name = "Mohan Das" }
]
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]